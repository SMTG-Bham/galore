language: python
cache: pip
notifications:
  email: false
python:
  - "2.7"
  - "3.4"
  - "3.6"
os:
  - linux
# command to install dependencies
install:
  - pip install gpaw
  - pip install .
# command to run tests
script:
  - python setup.py test
